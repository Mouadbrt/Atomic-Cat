# Joe's Coffee React

## Run

```bash
npm install
npm run dev -- --force
```

Open the Local URL shown by Vite.

## Notes
- CursorTrail was removed.
- Hero section was rebuilt like the reference screenshots.
- No node_modules, dist, or package-lock are included.
- .npmrc points to the official npm registry.
